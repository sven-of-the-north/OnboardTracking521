include 'pi', 'server', 'client'

rootProject.name = "ots"
project(":pi").name = "${rootProject.name}pi"
project(":server").name = "${rootProject.name}server"
project(":client").name = "${rootProject.name}webclient"
